import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  icon: {
    marginRight: "6pt",
    fontSize: "32px",
  },
  title: {
    display: "flex",
    alignItems: "center",
    margin: "24pt 0",
  },
}));
